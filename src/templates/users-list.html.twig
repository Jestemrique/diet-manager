<div class="container">
            <h2>Diet Planner. Usuarios</h1>
            <hr>
        
            <table class="table table-striped table-bordered">
                <thead>
                    <tr>
                      <th scope="col">
                        <input class="form-check-input" type="checkbox" id="checkboxNoLabel" value="" aria-label="...">
                        
                      </th>
                      <th scope="col">ID</th>
                      <th scope="col">Nombre</th>
                      <th scope="col">Email</th>
                    </tr>
                </thead>
                <tbody>
                    {# <?php foreach ($usersList as $user) { ?> #}
                     {% for user in userList %}
                        <tr>
                        <td>
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" id="{{user.user_id}}" name="user_id[]" value=" {{user.user_id}} ">
                            </div>
                        </td>    
                        <th scope="row">{{user.user_id}}</th>
                        <td>
                            <div>
                                {{user.user_name}}
                            </div>
                            <a href="?page=user-form&action=edit&user_id={{user.user_id}}">Editar</a>
                            <a href="?page=diet-users&action=delete&user_id={{user.user_id}}">Eliminar</a>
                        </td>

                        <td>
                            <div>
                                {{user.user_email}}
                            </div>
                        </td>


                        </tr>
                    {% endfor %}
                </tbody>
            </table>
            </div>
       